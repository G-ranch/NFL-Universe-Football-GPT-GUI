--// NFL Admin Panel v2.4 - Fully Functional
--// By ChatGPT

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local player = Players.LocalPlayer

-- Character references
local function getChar()
	local char = player.Character or player.CharacterAdded:Wait()
	local hum = char:FindFirstChildOfClass("Humanoid") or char:WaitForChild("Humanoid")
	local hrp = char:FindFirstChild("HumanoidRootPart") or char:WaitForChild("HumanoidRootPart")
	return char, hum, hrp
end

--// GUI Setup
local gui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
gui.Name = "NFLAdminPanel"

local panel = Instance.new("Frame")
panel.Size = UDim2.new(0,380,0,320)
panel.Position = UDim2.new(0.5,-190,0.5,-160)
panel.BackgroundColor3 = Color3.fromRGB(25,25,30)
panel.Active = true
panel.Draggable = true
panel.Parent = gui

local corner = Instance.new("UICorner", panel)
corner.CornerRadius = UDim.new(0.08,0)
local stroke = Instance.new("UIStroke", panel)
stroke.Thickness = 2
stroke.Color = Color3.fromRGB(80,120,255)
stroke.Transparency = 0.4

-- Title
local title = Instance.new("TextLabel", panel)
title.Size = UDim2.new(1,-40,0,40)
title.Position = UDim2.new(0,10,0,0)
title.BackgroundTransparency = 1
title.Text = "🏈 NFL ADMIN PANEL"
title.TextColor3 = Color3.fromRGB(200,220,255)
title.TextSize = 20
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left

-- Minimize Button
local minimizeBtn = Instance.new("TextButton", panel)
minimizeBtn.Text = "—"
minimizeBtn.Font = Enum.Font.GothamBold
minimizeBtn.TextSize = 26
minimizeBtn.TextColor3 = Color3.fromRGB(255,255,255)
minimizeBtn.Size = UDim2.new(0,40,0,40)
minimizeBtn.Position = UDim2.new(1,-45,0,0)
minimizeBtn.BackgroundTransparency = 1

-- Status Label
local statusLabel = Instance.new("TextLabel", panel)
statusLabel.Size = UDim2.new(1,0,0,40)
statusLabel.Position = UDim2.new(0,0,0,0)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = "Ready! 😈"
statusLabel.TextColor3 = Color3.fromRGB(100,255,100)
statusLabel.Font = Enum.Font.GothamBold
statusLabel.TextSize = 18

-- Button Creation
local function createButton(text)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0,280,0,45)
	btn.BackgroundColor3 = Color3.fromRGB(50,60,90)
	btn.Text = text
	btn.TextColor3 = Color3.fromRGB(255,255,255)
	btn.Font = Enum.Font.GothamBold
	btn.TextSize = 18
	btn.AutoButtonColor = false
	local c = Instance.new("UICorner", btn)
	local s = Instance.new("UIStroke", btn)
	s.Color = Color3.fromRGB(100,140,255)
	s.Transparency = 0.5
	btn.MouseEnter:Connect(function()
		TweenService:Create(btn,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(70,90,140)}):Play()
	end)
	btn.MouseLeave:Connect(function()
		TweenService:Create(btn,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(50,60,90)}):Play()
	end)
	return btn
end

-- Buttons
local stealBtn = createButton("🏈 STEAL BALL")
local magBtn = createButton("🧲 MAG")
local tackleBtn = createButton("💥 AUTO TACKLE")
local speedBtn = createButton("⚡ SUPER SPEED (128)")
local noclipBtn = createButton("🚫 NOCLIP")

stealBtn.Parent = panel
magBtn.Parent = panel
tackleBtn.Parent = panel
speedBtn.Parent = panel
noclipBtn.Parent = panel

local layout = Instance.new("UIListLayout", panel)
layout.Padding = UDim.new(0,8)
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
layout.VerticalAlignment = Enum.VerticalAlignment.Center
layout.FillDirection = Enum.FillDirection.Vertical

-- Functional Variables
local magnetActive = false
local magnetLoop
local speedOn = false
local noclipOn = false
local noclipConnection

--// Football Scanner
local function findFootball()
	for _, obj in pairs(workspace:GetDescendants()) do
		if obj:IsA("BasePart") and (obj.Name == "Football" or obj.Name:match("^Football %(.+%)$")) then
			local isHeld = obj.Parent:FindFirstChildOfClass("Humanoid") ~= nil
			local owner = nil
			if isHeld then owner = obj.Parent.Name end
			return obj, owner, isHeld
		end
	end
	return nil,nil,false
end

-- STEAL BALL
stealBtn.MouseButton1Click:Connect(function()
	local char, hum, hrp = getChar()
	local ball, owner, isHeld = findFootball()
	if not ball then
		statusLabel.Text = "❌ No football!"
		statusLabel.TextColor3 = Color3.fromRGB(255,50,50)
		return
	end
	-- Remove welds if any
	for _, w in pairs(ball:GetDescendants()) do if w:IsA("Weld") then w:Destroy() end end
	local hand = char:FindFirstChild("RightHand") or char:WaitForChild("RightHand")
	local weld = Instance.new("Weld")
	weld.Part0 = hand
	weld.Part1 = ball
	weld.C0 = CFrame.new(0,0,0)
	weld.Parent = hand
	statusLabel.Text = "🏈 Stole the ball!"
	statusLabel.TextColor3 = Color3.fromRGB(50,255,100)
end)

-- MAG
magBtn.MouseButton1Click:Connect(function()
	local char, hum, hrp = getChar()
	magnetActive = not magnetActive
	if magnetActive then
		statusLabel.Text = "🧲 Chasing ball..."
		magnetLoop = RunService.Heartbeat:Connect(function()
			local ball, owner, isHeld = findFootball()
			if not ball then
				magnetActive = false
				if magnetLoop then magnetLoop:Disconnect() end
				statusLabel.Text = "❌ No football!"
				statusLabel.TextColor3 = Color3.fromRGB(255,50,50)
				return
			end
			if ball:IsDescendantOf(char) then
				magnetActive = false
				if magnetLoop then magnetLoop:Disconnect() end
				statusLabel.Text = "🟢 Ball acquired!"
				statusLabel.TextColor3 = Color3.fromRGB(100,255,100)
				return
			end
			if hrp then
				local dir = (ball.Position - hrp.Position).Unit
				hrp.CFrame = CFrame.new(ball.Position - dir*3)
			end
		end)
	else
		if magnetLoop then magnetLoop:Disconnect() end
		statusLabel.Text = "Ready! 😈"
		statusLabel.TextColor3 = Color3.fromRGB(100,255,100)
	end
end)

-- AUTO TACKLE
tackleBtn.MouseButton1Click:Connect(function()
	local char, hum, hrp = getChar()
	task.wait(0.1)
	local ball, owner, isHeld = findFootball()
	if not ball then
		statusLabel.Text = "❌ No football!"
		statusLabel.TextColor3 = Color3.fromRGB(255,50,50)
		return
	end
	if ball:IsDescendantOf(char) then
		statusLabel.Text = "🟢 Already have the ball!"
		statusLabel.TextColor3 = Color3.fromRGB(100,255,100)
		return
	end
	local dir = (ball.Position - hrp.Position).Unit
	hrp.CFrame = CFrame.new(ball.Position - dir*1)
	statusLabel.Text = "💥 Auto Tackle!"
	statusLabel.TextColor3 = Color3.fromRGB(255,200,50)
end)

-- SUPER SPEED
speedBtn.MouseButton1Click:Connect(function()
	local char, hum, hrp = getChar()
	speedOn = not speedOn
	if speedOn then
		hum.WalkSpeed = 128
		speedBtn.Text = "⚡ SPEED: ON (128)"
		speedBtn.BackgroundColor3 = Color3.fromRGB(50,255,100)
	else
		hum.WalkSpeed = 16
		speedBtn.Text = "⚡ SUPER SPEED (128)"
		speedBtn.BackgroundColor3 = Color3.fromRGB(50,60,90)
	end
end)

-- NOCLIP
noclipBtn.MouseButton1Click:Connect(function()
	local char, hum, hrp = getChar()
	noclipOn = not noclipOn
	if noclipOn then
		noclipBtn.Text = "🚫 NOCLIP: ON"
		noclipBtn.BackgroundColor3 = Color3.fromRGB(50,255,100)
		if noclipConnection then noclipConnection:Disconnect() end
		noclipConnection = RunService.Stepped:Connect(function()
			for _, v in pairs(char:GetDescendants()) do
				if v:IsA("BasePart") then v.CanCollide = false end
			end
		end)
	else
		noclipBtn.Text = "🚫 NOCLIP"
		noclipBtn.BackgroundColor3 = Color3.fromRGB(50,60,90)
		if noclipConnection then noclipConnection:Disconnect() end
	end
end)

-- Minimize Icon
local nflIcon = Instance.new("TextButton")
nflIcon.Text = "🏈 NFL"
nflIcon.Font = Enum.Font.GothamBold
nflIcon.TextSize = 26
nflIcon.TextColor3 = Color3.new(1,1,1)
nflIcon.Size = UDim2.new(0,150,0,150)
nflIcon.BackgroundColor3 = Color3.fromRGB(60,100,255)
nflIcon.BackgroundTransparency = 0.1
nflIcon.Visible = false
nflIcon.Active = true
nflIcon.Draggable = true
nflIcon.Parent = gui

local icCorner = Instance.new("UICorner", nflIcon)
icCorner.CornerRadius = UDim.new(0.45,0)
local glow = Instance.new("UIStroke", nflIcon)
glow.Thickness = 2
glow.Color = Color3.fromRGB(140,180,255)
glow.Transparency = 0.3

-- Minimize behavior
minimizeBtn.MouseButton1Click:Connect(function()
	local pos = panel.Position
	TweenService:Create(panel,TweenInfo.new(0.3),{Size=UDim2.new(0,0,0,0),BackgroundTransparency=1}):Play()
	task.wait(0.3)
	panel.Visible = false
	nflIcon.Position = pos
	nflIcon.Visible = true
end)

-- Restore
nflIcon.MouseButton1Click:Connect(function()
	panel.Position = nflIcon.Position
	panel.Visible = true
	TweenService:Create(panel,TweenInfo.new(0.3),{Size=UDim2.new(0,380,0,320),BackgroundTransparency=0.2}):Play()
	nflIcon.Visible = false
end)
